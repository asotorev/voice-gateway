# Audio Embedding Processor - Lambda Dependencies Only
# NOTE: torch, numpy, scipy provided by layers
# NOTE: resemblyzer, soundfile, webrtcvad provided by consolidated layer

# Core AWS SDK
boto3>=1.28.0,<2.0.0
botocore>=1.31.0,<2.0.0

# Audio processing (heavy dependency, not suitable for layer)
librosa>=0.9.0,<1.0.0

# HTTP utilities
requests>=2.28.0,<3.0.0

# Logging
structlog>=22.1.0,<24.0.0

# Date/time handling
python-dateutil>=2.8.0,<3.0.0

# URL parsing
urllib3>=1.26.0,<2.0.0
