# Lambda function dependencies for audio embedding processor
# Core AWS SDK - Should match Lambda runtime
boto3>=1.28.0,<2.0.0
botocore>=1.31.0,<2.0.0

# JSON and HTTP utilities
requests>=2.28.0,<3.0.0

# Audio processing dependencies (for future ML integration)
# These will be moved to Lambda layers in later commits
numpy>=1.21.0,<2.0.0

# Logging and utilities
structlog>=22.1.0,<24.0.0

# Date/time handling
python-dateutil>=2.8.0,<3.0.0

# URL parsing and encoding
urllib3>=1.26.0,<2.0.0

# Development and testing dependencies (excluded in deployment)
# pytest>=7.0.0
# moto>=4.0.0  # AWS mocking for tests
