# Consolidated Voice Gateway Layer
# Combines: resemblyzer + audio-utils + shared-code
# NOTE: numpy, scipy provided by AWS SciPy layer
# NOTE: torch provided by PyTorch layer

# Voice embeddings
resemblyzer>=0.1.3

# Audio utilities (lightweight)
soundfile>=0.10.0,<0.13.0
audioread>=3.0.0
webrtcvad>=2.0.10
